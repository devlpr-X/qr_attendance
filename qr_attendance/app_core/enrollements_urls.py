# app_core/urls.py
from django.urls import path
from app_core.views.enrollment.enrollment import *

urlpatterns = [
    # path('enrollment/class-group-schedule/', session_generate, name='session_generate'),
]
